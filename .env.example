# ===========================================
# APP BRANDING
# ===========================================
NEXT_PUBLIC_APP_NAME=My SaaS App
NEXT_PUBLIC_APP_DESCRIPTION=Your micro SaaS application description

# ===========================================
# CONVEX - Auto-generated by `npx convex dev`
# ===========================================
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud

# ===========================================
# AUTHENTICATION - GitHub OAuth (Optional)
# Get from: https://github.com/settings/developers
# ===========================================
AUTH_GITHUB_ID=your_github_client_id
AUTH_GITHUB_SECRET=your_github_client_secret

# ===========================================
# CONVEX DASHBOARD ENVIRONMENT VARIABLES
# Set these in: Convex Dashboard > Settings > Environment Variables
# See convex-docs.md Section 9 for detailed instructions
# ===========================================
#
# REQUIRED FOR PASSWORD AUTH:
# JWT_PRIVATE_KEY=-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----
# JWKS={"keys":[{"kty":"RSA","n":"...","e":"AQAB","kid":"..."}]}
#
# Generate keys with:
#   openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:2048 -out private_key.pem
#   openssl rsa -in private_key.pem -pubout -out public_key.pem
#
# Convert to single line (for JWT_PRIVATE_KEY):
#   awk 'NF {sub(/\r/, ""); printf "%s\\n",$0;}' private_key.pem
#
# Generate JWKS from public key:
#   npx pem-jwk public_key.pem > jwks.json
#
# REQUIRED FOR OAUTH:
# SITE_URL=https://your-domain.vercel.app
# AUTH_GITHUB_ID=your_github_client_id
# AUTH_GITHUB_SECRET=your_github_client_secret
